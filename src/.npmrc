# Next.js standalone output copies node_modules as-is. pnpm's default symlinked
# layout does not survive artifact upload and Azure zip deployment, which breaks
# Next's runtime dependency resolution. A hoisted layout produces real directories.
node-linker=hoisted
